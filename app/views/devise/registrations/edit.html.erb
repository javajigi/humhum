<h2>계정 수정하기</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label "이름" %> <%= f.text_field :username, :autofocus => true %></div>
  <div><%= f.label "이메일 주소" %> <%= f.email_field :email %></div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div><%= f.label "현재 비밀번호" %> <%= f.password_field :current_password %></div>
  <br />
  <div><%= f.label "새 비밀번호" %> <span class="help-block">비밀번호를 바꾸고 싶을 때만 입력</span>
  <%= f.password_field :password %></div>
  <div><%= f.label "비밀번호 확인" %> <%= f.password_field :password_confirmation %></div>



  <div><%= f.submit "계정 수정", class: "btn btn-primary" %></div>

<% end %>

<h3>계정 삭제하기</h3>

<p>서비스에 불만족하십니까? <%= button_to "계정 삭제", registration_path(resource_name), class: "btn btn-danger", :data => { :confirm => "Are you sure?" }, :method => :delete %></p>
